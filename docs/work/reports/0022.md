# Goal 0022 — native PDF embedded text / TTS

## Result — A2 correction continuation

The A2 correction preserves the accepted native embedded-text path and repairs both director blockers:

- The single `PdfNativeService` now runs resumable one-page text steps, returning to arbitration between pages. Current raster requests are explicitly serviced before the next background text page; generation cancellation abandons stale jobs safely.
- A deterministic 40-page process-wide Pdfium probe proves text remains incomplete, Current raster completes first, text resumes/completes, and metadata/raster/text share the same owner thread.
- Native text preparation now builds normalized pages, canonical per-page sentences, counts, anchor state, search matches, and page provenance on a worker thread. The egui commit consumes the immutable payload without resplitting raw pages or synchronously writing the cache.
- Versioned cache serialization/persistence is dispatched on that worker before the prepared event; stale source/generation/revision and page-coverage checks remain bounded and visual-only failure-safe.

## Original implementation

Implemented the native trustworthy embedded-text path on the existing Goal 0019/0020 Pdfium owner.

- Extended the process-wide `PdfNativeService` with asynchronous page-aligned Pdfium text extraction.
- Results carry source identity, generation/revision, native page count, page payloads, worker-thread evidence, and terminal success/failure through the receiver result.
- Added a named deterministic trust gate for coverage, minimum density, replacement/control garbage, and duplicate-page noise.
- Visual PDF open remains independent; enrichment runs on the native worker and stale source generations are discarded.
- Added atomic `ReaderSession` adoption that preserves native PDF page identity and feeds existing sentence/search/text-only/TTS state without arbitrary repagination.
- Accepted text is persisted through the existing versioned `PdfRenderPrecomputedState` cache shape and reused on reopen.
- No Python, Docling, OCR, Quack-check, or external recovery process was added to the production path.

## Deterministic evidence

- `cargo check --workspace` passed.
- `cargo build --workspace` passed.
- `cargo test --workspace -- --test-threads=1` passed.
- Focused package tests passed: 202 core tests, 111 egui tests, and 83 app tests; controlled-font matrix, trust gate, prepared page provenance, session adoption, and stale-render coverage are green.
- Focused native lifecycle probe passed with a valid two-page embedded-text PDF, shared worker identity, raster rendering, extraction, and malformed-input rejection.
- Dedicated cooperative-arbitration probe passed with a 40-page valid PDF: incremental extraction remained incomplete, Current raster completed first, extraction resumed/completed, and all work shared the Pdfium worker identity.
- Trust-gate tests pass for meaningful text, empty/image-only, replacement garbage, and duplicate noise.
- Session adoption test passes for native page count, page-aligned sentences, search, and no repagination.
- Existing EPUB, pretty-layout, continuous PDF viewport, zoom, and Windows TTS tests remain green.
- `.\qa.ps1 -SkipDependencyCheck -PrepareOnly` passed and prepared `.qa/windows/fixtures/representative.epub`.

## Environment / hosted gate

The local `scripts/windows-dev-check.ps1` reports missing `cl.exe` and `MSBuild.exe` in this non-Developer-PowerShell shell; Rust MSVC, Cargo, CMake, Ninja, and Pandoc are available. The A2 branch must receive a fresh hosted workflow after the new cooperative-arbitration coverage is pushed; the prior A1 workflow `34893256515` is not used as A2 evidence.

No human QA was requested.
