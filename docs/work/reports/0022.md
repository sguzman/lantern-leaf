# Goal 0022 — native PDF embedded text / TTS

## Result — A4 correction complete

This report continues the same Goal 0022 branch/report lineage. A3’s accepted direction is preserved while the A4 director blockers are corrected:

- Trusted native text now promotes the canonical PDF session from render-only to full canonical text/search/TTS capability while retaining disabled exact visual sentence synchronization until geometry exists.
- Cache lookup, validation, and cache parsing now run on a detached worker. Cache hits and extraction misses re-enter the same stale-safe trusted preparation/adoption path.
- The enrichment commit consumes worker-prepared canonical sentences and a worker-built snapshot; egui only validates, moves prepared session ownership, and moves the already-built reader snapshot into runtime. It no longer constructs a document-scale snapshot during adoption.
- Full-text PDF search uses the complete canonical sentence domain and prepared page/local provenance, so Next/Previous navigates the owning native page without repagination or geometry ownership changes.
- Native metadata/text request submission uses `try_send` with detached retry dispatchers, so a saturated bounded queue cannot block the UI caller.
- Any queued raster request, including Nearby, is serviced through the normal Current-over-Nearby scheduler before another background text chunk.
- Text extraction uses bounded eight-page native document chunks, materially reducing document opens while preserving frequent raster arbitration.

## Preserved architecture

- Native Rust + eframe/egui + one process-wide Pdfium owner.
- Visual PDF open remains independent of enrichment.
- Quack-check, Python, Docling, OCR, scripts/quack-check, and external recovery remain outside this goal’s production path.
- Native text remains conservative, source/generation stale-safe, trusted-only, page aligned, cache-versioned, and connected to the existing canonical ReaderSession/TTS machinery.
- Goal 0019/0020 continuous rendering, zoom, scheduling, residency, and EPUB/TTS behavior remain covered by the repository tests.

## A4 deterministic evidence

- `cargo check --workspace` passed.
- Focused serialized core/egui tests passed: 203 core tests and 111 egui tests.
- Production-shaped render-only PDF policy promotion test passed: trusted adoption enables Text-only, full canonical search, ordinary TTS, and native page ownership while keeping exact visual sync disabled; the rejected/render-only session remains disabled.
- The same test searches a later page and verifies Next moves to that page through prepared provenance.
- Worker-built reader publication removed the egui-side prepared snapshot construction path; the live egui commit performs no `snapshot`/`snapshot_internal` call.
- Cache IO is only reached from the detached cache worker; cache hits still enter typed preparation/adoption and cache misses fall back to native extraction.
- Dedicated Current raster preemption probe passed.
- Dedicated Nearby raster preemption probe passed.
- Both probes verify shared metadata/raster/text worker identity and assert native document opens are fewer than the 40-page document’s page count.
- `git diff --check` passed.

## Final validation

- `cargo test --workspace -- --test-threads=1` passed: 203 core, 83 app, 111 egui, integration suites, and Windows TTS probes.
- `cargo check --workspace` passed.
- `cargo build --workspace` passed.
- `.\qa.ps1 -SkipDependencyCheck -PrepareOnly` passed.
- `git diff --check` passed.
- Dedicated local probes passed shared Pdfium lifecycle, Current arbitration, and Nearby arbitration.
- Fresh hosted Windows workflow `34910408356` passed workspace check/build/test, QA preparation, Windows TTS, notification, and hosted renderer gates; renderer job `104198301208` passed shared Pdfium lifecycle, Current, Nearby, and renderer capability probes.
- Terminal implementation commits: `982c87c` and `45617ab`.

No human QA is requested.
