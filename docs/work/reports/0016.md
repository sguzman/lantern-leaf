# Goal 0016 report — Starter library live-state continuity

Status: active
Attempt: A2
Implementation branch: `codex/0016-starter-library-live-state-continuity`

## Result

Implemented a background progressive Caliberate catalog path and same-session Recents refresh.

- Caliberate pages now publish bounded page batches while the existing worker continues the full provider walk.
- Starter state merges batches by book ID, rejects stale request batches/completions, preserves partial rows after provider failure, and marks complete only after the final catalog/cache event.
- The starter UI exposes partial/loading progress and makes clear that search/sort currently cover loaded rows during an incomplete refresh.
- Durable cache writing remains on the background catalog worker and the existing final ordering/cache path remains intact.
- After successful SourceOpen persistence, the app dispatches the existing background Recents listing command so the current Starter model updates without restart. Failed persistence does not trigger the refresh.
- No Caliberate sibling repository changes were needed.

## Validation

Focused validation is complete for the progressive worker callback, batch reducer merge/deduplication, stale-request rejection, partial-provider failure preservation, and successful-only Recents refresh trigger. `cargo check --workspace`, `cargo test --workspace -- --test-threads=1`, `cargo build --profile qa --bin lanternleaf`, and `./qa.ps1 -SkipDependencyCheck -PrepareOnly -ResetQaState` passed on Windows. The first QA-preparation invocation encountered an orphaned Caliberate test process holding the generated EPUB; after stopping that test process, the repository-owned preparation gate passed. No product or sibling-repository files were changed by that cleanup. Required CI run `34829094243` passed both native Windows workspace/TTS checks and the hosted renderer probe.

## Watcher and handoff

Goal 0016 A1 was rejected before human QA. A2 synchronized director `main` at `469cf5c`, preserved the accepted A1 implementation, moved the reopened goal back to `active/`, and re-armed the watcher before implementation. No human QA was requested.

## A2 correction scope

The A2 implementation will preserve live lazy-cover state during metadata/final catalog reconciliation, keep progressive provider failure visibly failed/degraded instead of converting it into ordinary success through stale-cache fallback, and establish one authoritative catalog-load owner so overlapping refresh workers cannot race provider walks or durable cache writes.
