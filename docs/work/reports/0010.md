# Goal 0010 report — Caliberate catalog covers and provider-availability UX

Status: active — A6 correction in progress
Attempt: A6
Implementation branch: `codex/0010-caliberate-catalog-reliability`

## A5 correction audit

The A5 review identified four implementation defects: lazy cover work owned the full catalog busy scope; provider failures had no book-identified event; no-cover/decode outcomes could remain pending; and the branch/report had been based on stale `main` with non-terminal CI evidence. A6 synchronized current LanternLeaf `main` (`d804843`) before replaying the implementation, and preserved the accepted Goal 0014 closure and Goal 0015 queue state.

## Result

Implemented the Caliberate catalog cover contract and lazy catalog presentation without changing the withdrawn 42866 materialization interpretation. The worker inspected the live Caliberate API first: it exposed `has_cover` metadata but no cover bytes or stable cover endpoint. A minimal sibling-repository contract was added and pushed at `sguzman/caliberate` commit `97a41b0` on branch `codex/0010-cover-endpoint`.

LanternLeaf now carries `has_cover` through the Caliberate/catalog/app contracts, uses only the explicit Caliberate route `/api/v1/books/{id}/cover`, and keeps legacy thumbnail routes limited to the legacy Calibre provider. Caliberate catalog loading does not prefetch remote covers or materialize EPUBs. Visible catalog rows schedule at most four cover requests; request, disk, and image decoding work stays in the effect/thumbnail workers. Successful thumbnails are cached, while provider failures remain retryable in memory and are never written as permanent negative cache entries.

The starter catalog now distinguishes loading, no advertised cover, provider-unavailable, and cover-unavailable states. Provider errors are surfaced with a provider-specific bridge error and the library remains recoverable through refresh/retry. Existing local sidecar, embedded EPUB, Recents, and legacy Calibre paths remain intact.

A6 replaces the shared catalog busy ownership with a book-identified `CalibreCoverCompleted` event stream. Every started request now emits a terminal loaded, cover-unavailable, provider-unavailable, or cover-error outcome; the UI removes that book from pending, records retry backoff for transient provider failures, and updates the specific catalog row on success. Provider image decode/cache failures are propagated instead of being collapsed into an indefinite loading state. Added reducer/planning regression tests cover the no-scope and terminal-outcome invariants.

## Validation

- `cargo check --workspace` — passed.
- `cargo test --workspace -- --test-threads=1` — passed: 195 core, 75 app, 79 egui, integration/parity/doc tests, and Windows TTS probes.
- `cargo build --profile qa --bin lanternleaf` — passed.
- `qa.ps1 -SkipDependencyCheck -PrepareOnly -ResetQaState` — passed; Windows QA fixtures and staged configuration prepared.
- Sibling Caliberate `cargo test -p caliberate-server --lib --tests` — passed before the cover-contract push (2 library, 10 API, 3 auth, 6 OPDS tests).

### A6 correction validation

- Current sibling `main` was synchronized from `29bd0a7`; the cover endpoint was replayed as `415fd07` and targeted post-change tests were added in `3799cca` on `codex/0010-cover-endpoint-a6`.
- `cargo test -p caliberate-server --lib --tests` after the replay/tests — passed: 3 library, 11 API, 3 auth, 6 OPDS tests.
- LanternLeaf focused correction tests — passed: app 77, egui 79, core 195.
- LanternLeaf `cargo test --workspace -- --test-threads=1` — passed, including integration/parity and Windows TTS probes.
- The previous A5 CI run `34777773736` is now completed successfully, but A6 requires a new CI run for the pushed correction branch; it has not yet been started because the correction commit is still local.

The first parallel workspace test invocation exposed an existing environment-sensitive config test race; the same suite passed serialized with `--test-threads=1`, and the targeted test passed independently.

## Watcher and handoff

The Goal 0010 watcher was re-armed with `-Rearm` for A6 before implementation. Terminal signaling remains pending until the A6 implementation branch is pushed and its required Windows CI run succeeds. No human QA was requested during implementation.
