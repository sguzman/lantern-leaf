# Goal 0020 — Continuous native PDF viewport and practical zoom

## Attempt

Attempt A1 on branch `codex/0020-continuous-pdf-viewport-and-zoom`, based on director `main` commit `484ec5b`.

## Delivered

- Replaced the single-page PDF presentation with a continuous vertical egui page stack.
- Added deterministic page-slot geometry with bounded visible/overscan selection, stable center-anchor current-page derivation, mixed aspect-ratio support, and intra-page jump anchors.
- Kept native raster work on the existing PdfRenderWorker/Pdfium ownership path; placeholders and geometry are handled on the egui thread.
- Preserved source/generation/page/render-size identity and source-switch invalidation. Visible pages remain pinned by existing residency rules.
- Added explicit continuous-stack jump targeting for Previous/Next controls.
- Expanded zoom stepping to 25–400% and added Fit width, Fit page, Reset/100% state. Logical page geometry updates immediately while native rasters catch up asynchronously.
- Preserved existing EPUB, TTS, pretty-rendering, scheduler, and native service behavior.

## Validation

- `cargo test -p lanternleaf-egui -- --test-threads=1` — passed: 103 passed, 1 ignored.
- `cargo test --workspace -- --test-threads=1` — passed.
- `cargo check -p lanternleaf-egui` — passed.
- `cargo build --workspace` — passed.
- `git diff --check` — passed.
- `./qa.ps1 -PrepareOnly -SkipDependencyCheck` — passed.
- `./scripts/windows-dev-check.ps1` — reports missing `cl.exe` and `MSBuild.exe` in this shell; Rust/Cargo/native test execution remained available.
- Windows QA TTS synthesis/config test — passed.
- Windows TTS probe — passed.
- Dedicated native Pdfium lifecycle probe — passed.
- Local hosted-renderer equivalent — native `lanternleaf.exe` remained alive for the probe window.

The hosted `windows-baseline` workflow remains required for the MSVC-hosted native-workspace and hosted-renderer jobs after push.

## Watcher

Goal watcher was started for Goal 0020 after moving the contract to `docs/work/active/`. Terminal signaling is deferred until the terminal branch is pushed.

## Terminal state

Acceptance gates satisfied by deterministic/local validation. Physical QA was not requested during implementation, per the goal contract.
