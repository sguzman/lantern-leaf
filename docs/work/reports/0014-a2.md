# Goal 0014 — Reader presentation-geometry anchor stability

## Attempt A2 correction

This correction started from authoritative director `main` `c06114e`, read the generalized ready contract and `docs/work/reviews/0014-a1-director-rejection.md`, and continued on the required branch `codex/0014-reader-presentation-anchor-stability`. The goal was activated in `89aac4b`; the watcher was re-armed for this attempt as PID `6836`.

The valid A1 lifecycle was preserved: before a `pretty_page` geometry-key transition invalidates measured heights, the persisted egui viewport is captured; after reflow, the viewport is restored from semantic state; a legitimate pending canonical TTS follow suppresses idle restoration and keeps existing follow ownership authoritative.

The correction replaces A1's absolute within-block pixel offset. When production pretty/canonical targets provide a segment, the anchor records canonical sentence identity and a normalized position within that sentence. Restoration resolves the same canonical segment in the reflowed mapping and computes the new block-relative location. If no canonical segment is available, it falls back to normalized fractional position within the same block. This handles materially rewrapping paragraphs while retaining bounded virtualization and natural post-follow behavior.

Deterministic production-adjacent coverage includes non-uniform materially rewrapping blocks at multiple positions, canonical sentence continuity, normalized fallback positions, horizontal/content-width, text-metric, block/heading-spacing, media, compound geometry classes, idle reading, and active-follow precedence. Existing media binding/non-binding aspect-ratio tests and Goal 0012 presentation/highlight tests remain intact.

Validation:

- Focused reader suite: 20 tests passed in both egui library and binary targets.
- `cargo test --workspace -- --test-threads=1`: passed with zero failures.
- `cargo check --workspace`: passed.
- `cargo build --workspace`: passed on Windows.
- `.\qa.ps1 -SkipDependencyCheck -PrepareOnly`: passed.
- Renderer smoke `lanternleaf.exe --renderer-smoke`: remained running through the 8-second probe and was stopped cleanly.
- `git diff --check`: passed.
- No human QA was requested.

GitHub Actions `windows-baseline.yml` run `34733109955` passed. Both the native Windows workspace/TTS job and hosted renderer-capability probe completed successfully.
