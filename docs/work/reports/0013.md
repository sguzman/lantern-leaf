# Goal 0013 — Starter shell responsive panel containment

## Attempt A1

Goal 0013 synchronized director `main` at `2b79997` before implementation and continued on branch `codex/0013-starter-shell-panel-containment`. The starter-shell containment implementation is `58ae9de`; the goal was activated in `9a3ea45`, and the Goal 0013 watcher was re-armed for this attempt (PID 42804).

## Result

Accepted and terminalized. The implementation is `58ae9de` on `codex/0013-starter-shell-panel-containment`, based on synchronized director `main` `2b79997`. Goal activation is `9a3ea45`; the watcher was re-armed for this attempt with PID `42804`.

The starter shell now chooses a deterministic two-column layout from the actual center width at a `1120px` breakpoint, with a complete one-column fallback below it. Recents, Calibre, and browser-tab areas remain reachable at narrow widths. Owned groups constrain their child widths; prose wraps, long paths/URLs truncate, and action rows wrap. Existing bounded list virtualization/scroll policies and off-thread work were preserved.

Deterministic validation:

- `cargo test -p lanternleaf-egui starter::tests -- --test-threads=1`: passed, 2 tests in the library and 2 in the binary target.
- `cargo test --workspace -- --test-threads=1`: passed; all workspace unit, integration, and doc-test targets completed with zero failures.
- `cargo check --workspace`: passed.
- `cargo build --workspace`: passed on Windows.
- `.\qa.ps1 -SkipDependencyCheck -PrepareOnly`: passed; repo-native fixtures/config prepared.
- Renderer smoke: `lanternleaf.exe --renderer-smoke` remained running through the 8-second probe and was stopped cleanly.
- `git diff --check`: passed.
- `cargo fmt --all -- --check`: reports pre-existing repository-wide formatting drift outside this goal; the changed starter file was formatted directly with rustfmt and no goal diff whitespace errors remain.

No human QA was requested during implementation.
