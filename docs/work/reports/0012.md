# Goal 0012 implementation report

## Result

Goal 0012 is complete on `codex/0012-pretty-presentation-controls-and-inline-images`.
The native egui reader now has a separate Presentation settings surface, live native rendering semantics, layered per-book persistence/reset behavior, and inline EPUB image rendering with bounded off-render-thread preparation and decode.

## Presentation controls and persistence

- Added a bounded, discoverable Presentation section separate from TTS controls.
- Wired family, weight, font size, line spacing, horizontal/vertical margins, word spacing, letter spacing, day/night highlights, pretty base scale, paragraph/block spacing, heading scales, and media limits into the existing `ReaderSettingsPatch` and `BookReaderOverrides` layering.
- Registered supported font-family/weight aliases during startup so changing family/weight selects actual native egui font faces without render-frame discovery or file I/O.
- Added `Use app presentation defaults`, which clears only presentation overrides and restores the app presentation defaults; TTS/backend/voice fields remain intact.
- Added a core regression proving presentation patches persist at book scope and reset without changing TTS backend settings.

## Pretty rendering and canonical identity

- Pretty block construction and source-to-target alignment now run on a bounded worker queue; the egui frame only consumes ready results and requests repaint.
- Margins, line spacing, word spacing, letter spacing, font family/weight, base scale, paragraph/block spacing, heading scales, and media limits affect rendering.
- Existing source-born canonical sentence IDs, ranges, highlight, follow, text-only behavior, rich inline styles, and bounded active-window rendering remain preserved.
- Added aspect-ratio/max-size regression coverage.

## EPUB/native image provenance

- Image records now retain raw references, normalized EPUB paths, chapter index, source order, extracted path, aliases, and optional alt text.
- Relative, nested, percent-encoded, fragment/query-bearing, and case/path-normalized references resolve through the EPUB extraction root only; process-current-directory and arbitrary `PathBuf::exists()` fallback are not used.
- Unresolved/corrupt images remain visible as bounded alt/source placeholders and emit concise diagnostics.
- Added a valid generated EPUB fixture with two spine chapters, PNG and encoded-space JPEG assets, nested directories, relative references, inline text around images, alt text, duplicate occurrence ordering, extraction-root containment, and 1x1 PNG/JPEG dimension assertions.

## Lazy image pipeline

- Image file reads and decoding remain in a single worker with a bounded request queue.
- Successful textures use bounded LRU-style eviction; transient failures use a five-second negative cache.
- The render thread performs only nonblocking ready-result consumption and egui texture upload, then requests repaint.
- No image decoding occurs at book open or for all images in a document.

## Validation evidence

- `cargo test --workspace -- --test-threads=1` passed locally: core 194, app 75 plus integration suites, egui 55 in library and binary targets, parity tests, Windows probes, and doctests.
- Focused persistence, EPUB provenance/dimension, image placeholder, aspect-ratio, canonical alignment, and bounded pretty-render tests passed.
- `cargo check --workspace` passed.
- `git diff --check` passed.
- `qa.ps1 -SkipDependencyCheck -PrepareOnly` passed on Windows.
- Final GitHub Actions Windows baseline run `34628740493` passed, including staged QA/synthesis, watcher-policy test, workspace check/build/test, Windows TTS probe, and hosted renderer capability probe.
- Repository-wide rustfmt check still reports pre-existing formatting drift in legacy/touched files; broad unrelated formatting was not rewritten. Compilation, tests, and diff policy checks pass.
- No human QA was requested during implementation.

## Handoff

- Terminal implementation commits: `03a315b`, `b60f83b`.
- Branch pushed to `origin/codex/0012-pretty-presentation-controls-and-inline-images`.
- Watcher was started for Goal 0012 with re-arm semantics before implementation.
- Goal is ready to move from `active/` to `done/`; terminal signal follows the successful push.

## Attempt A2 correction

This director correction preserved the accepted A1 implementation and closed the remaining evidence gaps without changing the TTS architecture or requesting human QA.

- The generated EPUB regression now exercises the production `load_book_content` path and asserts the resulting `StructuredDocument` contains the inline image block and surrounding canonical sentences, in addition to asset extraction, source order, aliases, containment, and PNG/JPEG dimensions.
- Image lookup now consumes `ReaderImageRef.normalized_path` and removes URL query/fragment suffixes before path normalization. Percent-decoding is UTF-8 safe for both EPUB provenance and pretty-reader lookup.
- Layered configuration coverage now proves app defaults, one book's presentation overrides, an untouched second book, reset-to-app presentation values, and preservation of that book's TTS backend and Windows voice.

Validation for A2:

- `cargo test --workspace -- --test-threads=1` passed locally: core 195, app 75 plus integration suites, egui 56 in library and binary targets, parity tests, Windows probes, and doctests.
- Focused EPUB production-chain, normalized image-resolution, canonical inline-image alignment, and layered persistence tests passed.
- `cargo check --workspace`, `qa.ps1 -SkipDependencyCheck -PrepareOnly`, and `git diff --check` passed.
- `cargo fmt --all -- --check` remains red only because of repository-wide pre-existing formatting drift; no broad unrelated formatting was introduced.
- Pushed Windows baseline workflow `34652876171` passed both `native-workspace` and `hosted-renderer-probe`, including the Windows TTS probe, watcher-policy test, workspace validation, and hosted renderer capability probe.

The A2 correction commit is `f952ad6`. The Goal 0012 watcher was re-armed for this attempt and remained active through terminalization.
