# Goal 0009 — A2.1 Director Acceptance

Status: **ACCEPTED AND INTEGRATED FOR FOCUSED REAL-DESKTOP SIGNOFF.**

A2.1 implementation: `b5e348f06a1ff730d2363dc61bc4ac864d871f07`.

A2.1 worker terminal: `66091555e41f03fe2fbce049c8d773024af55425`.

Authoritative Windows CI: `34532877674` — `native-workspace` and `hosted-renderer-probe` both passed.

## Director findings

A2.1 supplies the deterministic lifecycle evidence missing from A2 while preserving the useful A2 production corrections.

- The production pretty -> text-only transition helper is now exercised with an active canonical cursor, pending follow re-arm, 48 subsequent cursor transitions, page changes, follow decision/commit, Pause retention, and return-to-pretty canonical identity.
- The actual text-only renderer uses the same canonical row projection for `selectable_label(selected, ...)` and for auto-scroll consumption, so visible selection and follow share one identity.
- The production lifecycle handshake gates Close book on SessionClose persistence completion and gates Safe Quit on SafeQuit persistence completion; failure leaves the destructive transition unarmed, while successful Safe Quit arms exactly one native viewport close.
- Production Close book ordering is confirmation -> TTS Stop -> persistence -> CloseReaderSession -> Starter. The confirm modal is non-destructive until its Close book button dispatches the command.
- Stale TTS playback from an old or closed source is rejected against the currently active reader source.
- The left reader panel remains resizable with a 240 px minimum and 460 px maximum; 300+ character Piper diagnostics use the same bounded production presentation policy and retain the full wrapped message.
- Failed Piper selection is transactional in the deterministic runtime seam and now proves same-session recovery all the way through Windows settings -> Play -> first boundary/progress without source reopen.
- The A1 no-repeat continuation cursor, layered Zira/app/book voice configuration, Goal 0008 source-born EPUB synchronization, bounded rendering, and lightweight TTS hot paths remain green.

## Validation

Authoritative Windows run `34532877674` passed:

- repo-native Windows QA preparation;
- staged Windows QA configuration and synthesis;
- workspace check/build/test;
- Windows TTS probe;
- hosted renderer probe.

The worker report records app 75, core 191, egui 54 in both library/binary targets plus integration/parity/doctest coverage and focused A2.1 lifecycle/text-only/diagnostic/stale-source/Piper-recovery tests.

## Remaining real-desktop evidence

One focused Windows pass remains before Goal 0009 closes:

1. Open the same large EPUB and verify pretty playback still works without duplicate ordinary lines.
2. Switch from pretty to text-only while TTS is actively speaking; the audible sentence must immediately be visibly selected/highlighted and the sentence list must auto-follow subsequent boundaries.
3. Pause/resume in text-only; the visible row must remain stable while paused and continue correctly after resume.
4. Switch back to pretty; canonical highlight/follow identity must remain correct.
5. Trigger the missing-Piper error and confirm the left panel remains bounded/resizable rather than expanding to the full path/message width.
6. Without closing the book, return to Windows TTS and press Play; playback must resume successfully in the same reader session.
7. Use Close book; after confirmation it must stop TTS, persist, and return to the Starter/library without exiting LanternLeaf.
8. Reopen a book, use Safe Quit, confirm it, and verify the native app actually exits after persistence.

If these pass, Goal 0009 / Gate 2.6 closes and Gate 3 native PDF visual stability may be authorized.
