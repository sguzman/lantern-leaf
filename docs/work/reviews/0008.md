# Goal 0008 Director Review — A8.1 REJECTED; A8.2 STRUCTURED->PRETTY IDENTITY CORRECTION REQUIRED


## Current decision — attempt A8.2

**A8.1 REJECTED FOR INTEGRATION.**

Rejected worker terminal: `0bc6b53f7daa6e71f42ee6a7bb6c8d647e65a070`

A8.1 implementation: `e01cdf38a392cc31d6fbe16b884a1f5011ff67f6`

Windows CI: `34417953850` — fully green.

A8.1 successfully fixes the earlier fake source-provenance layer in core and the audio-boundary payload:

- neutral StructuredDocument/Block/Sentence model exists;
- canonical TTS text is projected from structured source sentences;
- prepared audio and first-sample events carry explicit canonical display ID;
- empty audio sources do not emit false first-sample boundaries;
- active repaint and semantic SentenceStarted remain.

Integration is withheld because the final source->pretty seam is still not identity-safe.

The egui renderer independently reparses raw HTML into its own PrettyBlock Vec and then treats `StructuredBlock.block_id` as if it were that Vec index. Those block sequences can diverge through HRs, tables, nested lists, inline-image paragraph splitting, and other renderer-only block decisions. The UI also discards source sentence ranges and re-splits pretty block text to recover them.

Additionally, semantic boundary IDs are global canonical identities but are written directly into a historically page-local `highlighted_display_idx`, and structured page partitioning still uses sentence-string search.

A8.2 makes the structured document itself the native pretty source, carries explicit source block/range identity into PrettyBlock/PrettySentenceTarget, gives ReaderSession a first-class canonical highlight identity, removes string-based structured repartitioning, and strengthens the real EPUB/runtime tests around exactly these seams.

No human QA until A8.2 passes director review.

## Historical decision — attempt A8.1

**FIRST A8 IMPLEMENTATION REJECTED FOR INTEGRATION.**

Rejected worker terminal head: `0b5ede68f3ee7b207db6cd179ee79f02d53937a2`

Rejected implementation: `b60cfbbb3483f0f9e52491f9ee766e190b7d4f49`

Windows CI: `34411324619` — green but insufficient for the A8 identity contract.

### Good A8 work to preserve

- actual Rodio sentence sources have first-sample markers;
- predicted-duration sleeping no longer drives visual cursor advancement;
- semantic SentenceStarted events exist and avoid generic progress coalescing;
- active TTS schedules egui repaint at ~24 ms;
- previous performance/backend/provider work remains green.

### Rejection reasons

1. The claimed source-born EPUB provenance is still generated by post-hoc normalized string matching. The loader first builds whole-document `tts_text`, then re-extracts text from regex-matched HTML blocks and searches forward through canonical sentence strings before stamping `data-ll-sentence-ids`.
2. No neutral first-class structured document/sentence provenance model was added to `LoadedBook`/core; guessed IDs embedded in HTML are carrying the mapping.
3. Native HTML is worse: its canonical TTS stream comes from Pandoc while provenance annotation uses a separate html2text stream.
4. Prepared audio items contain display IDs, but the actual marker sends only audio index and the runtime remaps through current mutable session plan instead of carrying canonical identity directly.
5. The real EPUB fixture is too small/simple and proves only attribute/anchor presence, not identity continuity. It does not cross TTS windows or cover nested spans/entities/distant duplicates/image/quote as required.
6. The 128-boundary test directly invokes the session boundary setter over two pages; it is not a runtime/audio-boundary integration test and does not cover required resume/repeat/next/prev semantics.

A8.1 keeps the good audio-marker/repaint work but requires a true one-pass structured EPUB extraction with direct canonical IDs, explicit boundary identity payloads, a stronger >128-sentence real EPUB fixture, and a controllable runtime-level simulated boundary driver.

No human QA until A8.1 passes director review.

## Historical decision — first A8 implementation

**REOPENED. A7/A7.1 FAILED REAL-DESKTOP SPOKEN-SENTENCE SYNCHRONIZATION.**

The same large real EPUB now has reliable Windows audio and good native UI performance, but pretty highlighting/follow remains effectively nonfunctional. Text-only mode partially follows but can lag audible speech by multiple lines.

Director inspection finds this is deeper than A7's follow-state logic:

1. **No egui TTS repaint contract exists.** Background TTS events do not schedule native frames, so the UI can remain stale until unrelated activity causes repaint.
2. **Cursor timing is duration-driven rather than audio-boundary-driven.** Rodio receives a queued sentence batch while the runtime advances visual cursor using predicted sentence durations and sleeps.
3. **EPUB pretty identity is still reconstructed post-hoc.** Ingestion independently produces `reading_html` and `html2text -> tts_text`; egui independently produces pretty blocks; A7 then exact-matches normalized sentences with bounded lookahead. Real EPUB structural divergence can leave most targets unmapped.

A8 replaces the remaining heuristic chain with source-born canonical sentence provenance carried through ingestion/TTS/pretty rendering, semantic audio `SentenceStarted` boundary events, and an explicit egui repaint policy during active playback.

A7/A7.1's bounded renderer and follow-state work should be preserved where compatible, but native EPUB identity must no longer depend on render-time string alignment.

No human QA until A8 is implemented, validated, and director-reviewed.

## Historical decision — attempt A7.1

**ACCEPTED AND INTEGRATED — REAL-DESKTOP PRETTY HIGHLIGHT/FOLLOW SIGNOFF PENDING.**

Accepted worker terminal head:

`609cc1ce52da039e585c418371f7210a2fd79e92`

A7 implementation:

`220082d0a2a946c476b1d09586cdaa65e22e3b64`

A7.1 correction implementation:

`d65a141cc801f104e0feae67c33a870f23ce4e64`

Authoritative Windows CI:

`34403241270`

### Director findings

The combined A7/A7.1 lineage now satisfies the architectural correction required by the real-desktop highlight failure.

- Native pretty synchronization is keyed by canonical global display-sentence index via ordered `Vec<Option<PrettySentenceTarget>>`, not normalized sentence text as global identity.
- Duplicate sentence strings retain distinct ordered occurrences.
- Native HTML TTS highlighting/follow does not fall back to proportional HTML anchors when canonical alignment is missing; unmapped sentences remain unmapped instead of causing a remote jump.
- Representable paragraph/quote targets carry sentence character ranges for sentence-level background highlighting, with bounded block-level fallback for unsupported geometry.
- Auto-scroll now carries a source/document + canonical sentence target and is cleared only on successful commit or supersession.
- TTS runtime follow requests are created from actual canonical display-cursor transitions via the production `follow_target_for_transition` rule rather than generic TTS-event presence.
- A7.1 repairs explicit Jump-to-highlight so the current authoritative canonical target can be deliberately re-armed even after a previous successful follow.
- Committed duplicate-follow identity is source-aware.
- Per-block measured heights and kind-specific estimates feed prefix-sum bounded virtualization rather than one permanent global block height.
- Bounded render windows remain in place; no full-book egui layout was reintroduced.

### Regression evidence

The egui regressions include:

- 10,500 canonical sentences across 1,500 blocks;
- widely separated identical sentence occurrences mapped to the correct ordered blocks;
- normalization differences and deliberately unmappable canonical sentences;
- bounded target-aware pretty windows;
- variable-height prefix geometry;
- durable pending target / supersession / commit behavior;
- explicit Jump-to-highlight re-arm;
- cross-document same-index duplicate protection;
- **128 sequential canonical cursor transitions** through the same production follow-decision helper used by runtime handling;
- Next, Prev, page transition, Pause/Resume/Repeat, unchanged voice/backend/settings semantics;
- `auto_scroll_tts=false` suppression of automatic follow.

Authoritative Windows run `34403241270` passed both `native-workspace` and `hosted-renderer-probe`, including QA preparation, staged Windows TTS synthesis/decode, workspace check/build/test, Windows TTS diagnostics, and renderer probing.

### Remaining evidence

The next evidence must be the same real large EPUB that exposed the defect.

The human should verify sustained behavior over multiple consecutive spoken sentences, not merely one initial jump:

1. visible highlight corresponds to the audible sentence;
2. viewport follows forward without random remote/backward jumps or oscillation;
3. an occasional unmapped sentence degrades by not jumping rather than jumping incorrectly;
4. Next/Prev follow exactly once to the new sentence;
5. Pause/Resume at the same cursor does not cause a new jump;
6. changing the Windows voice while staying on the same sentence does not move the viewport;
7. explicit Jump-to-highlight re-centers the current highlighted sentence.

If this passes, Goal 0008 / Gate 2.5 can finally close. If it fails, the new canonical-target/follow diagnostics should localize whether the fault is alignment identity, virtual geometry, or scroll commit behavior.

## Historical decision — first A7 implementation

**REOPENED. A6 WINDOWS SPEECH/VOICE VERIFICATION PASSED; PRETTY SPOKEN-SENTENCE SYNCHRONIZATION FAILED REAL-DESKTOP SIGNOFF.**

Real-desktop A6 evidence now proves:

- normal repo-native QA selects Windows TTS;
- physical Windows speech is audible;
- installed Windows voices can be selected and audibly changed;
- large Caliberate EPUB open and native interaction performance remain fast/snappy.

The remaining Gate 2.5 defect is severe pretty-view highlight/viewport synchronization. The human observes the visual highlight sometimes tracks the spoken sentence but also jumps to unrelated locations, alternates between correct/wrong positions, and sometimes fails to follow.

Director localization shows the canonical TTS cursor itself progresses coherently while the pretty layer uses unsafe identity/fallback rules:

1. normalized sentence text -> one block `HashMap` loses occurrence identity for duplicates;
2. missed HTML text matches fall back to a proportional HTML anchor map;
3. auto-scroll is a one-shot boolean consumed before successful target resolution/render/scroll;
4. generic TTS event presence triggers follow instead of canonical display-cursor changes;
5. bounded pretty virtualization uses one constant height estimate for heterogeneous blocks.

A7 requires first-class canonical display-sentence -> ordered pretty-target alignment, no proportional TTS fallback, durable target-aware follow state, canonical-cursor-driven scrolling, and stable variable-height bounded virtualization.

A6 remains accepted. Goal 0008 remains open solely because reader/TTS presentation parity is not acceptable yet.

No human QA until A7 terminalizes and receives director review.

## Historical decision — attempt A6

**ACCEPTED AND INTEGRATED.**

Accepted worker terminal head:

`114f375d58c7301df078e57a861ecaa2eff42226`

Authoritative A6 implementation commit:

`b0d0987166880301175599f783d271d40640ee5f`

A6 diagnostic-counter correction commit:

`15fb75cb1d17675c0c09b5a6c6b469aeffc58267`

Authoritative Windows CI:

`34304570627`

### Director findings

A6 closes the configuration defect that caused the A5.1 real-desktop silence while preserving the successful native-reader performance work.

- Omitted TTS backend selection now goes through one platform-aware `default_tts_backend()`: Windows -> `Windows`, non-Windows -> `Piper`.
- `TtsBackend::default`, `AppConfig::default`, table serde defaults, and table defaults all use that same source of truth.
- Explicit backend values remain authoritative in normal configuration; explicit Piper remains supported on Windows.
- Portable Piper defaults no longer encode a developer profile or Linux-only absolute path.
- Normal `qa.ps1` defaults `-TtsBackend Windows` and exports `LANTERNLEAF_QA_TTS_BACKEND=windows` after staging. Because the override is applied by the real config loader after parsing, pre-existing `.qa/windows/conf/config.toml` state cannot silently switch the QA run back to Piper.
- Piper remains available in QA only through the explicit `-TtsBackend Piper` override.
- The QA shell prints the effective intended backend before launch.
- TTS runtime failures are converted into actionable persistent native error notifications identifying the selected backend and classifying voice, Piper model/configuration, audio-output, or generic synthesis/runtime failures.
- The normal QA tracing filter retains LanternLeaf debug detail while suppressing html5ever tree-builder debug spam.
- QA handoff error extraction now includes TTS/Piper/voice/audio/synthesis terms.

The staged Windows QA integration test loads the actual staged config through the real config parser, verifies effective Windows backend selection, enumerates/resolves an installed Windows voice, synthesizes a deterministic sentence to WAV, and decodes that WAV with the shared Rodio path.

The first Windows A6 run exposed a process-global diagnostic-counter race inherited from A5.1 tests. Commit `15fb75cb...` made that snapshot diagnostic session-owned. Authoritative run `34304570627` tested that corrected implementation head and passed:

- repo-native QA preparation;
- staged Windows QA config + synthesis/decode;
- workspace check;
- workspace build;
- normal workspace tests;
- Windows TTS probe;
- hosted renderer probe.

### Real-desktop evidence already accepted

The A5/A5.1 real desktop run positively established that the previously unusable large EPUB is now substantially responsive / “pretty snappy.” That portion of Gate 2.5 is accepted and does not need to be rediscovered.

### Remaining evidence

Only physical speaker / interactive TTS signoff remains:

1. pull accepted `main`;
2. run `.\qa.ps1` with no backend flags;
3. open the same large Recent/Caliberate EPUB;
4. press Play without manually selecting a backend;
5. confirm Windows speech is audible;
6. verify next/previous/pause/resume/stop.

If that succeeds, Goal 0008 / Gate 2.5 can close. If it fails, use the now-visible native error and focused QA handoff diagnostics as the next evidence.
## Historical decision — attempt A5.1

**ACCEPTED AND INTEGRATED.**

Accepted worker terminal head:

`e08612666fddc3d9df1ed297ddb20f67dc50be0b`

Authoritative A5.1 implementation commit:

`196dc387de11cc36048d163686a23f94bbc097a0`

Authoritative Windows CI:

`34280238462`

### Director findings

A5.1 closes the ownership/hot-path defects that caused the first A5 rejection while preserving the useful A5 performance work.

- Production egui now constructs `TtsRuntime` with the same canonical `Arc<Mutex<Option<ReaderSession>>>` owned by the effect context. The old source-path-based cloned-session synchronization path is gone.
- Normal reader effects, TTS worker control/progression, and persistence therefore observe and mutate one ReaderSession value.
- `ReaderSession::playback_view` and `apply_command_lightweight` expose cursor/TTS/stats/settings state without constructing the heavyweight document snapshot.
- TTS command application, first playback-plan construction, repeated plan collection, pause/error transitions, and runtime seek/progress stepping use the lightweight path.
- Full `ReaderSession::snapshot` has a diagnostic construction counter. The 10,488-sentence worker regression submits Play plus 100 seek-next controls and proves the count remains zero.
- A shared-session regression mutates the cursor through the normal reader path, immediately runs Play-from-highlight through TTS, then seeks through TTS and reads the result back from the same canonical session.
- Persistence now derives source path, bookmark, config, and optional lightweight playback state directly from the canonical session. It no longer manufactures a UI ReaderSnapshot.
- The 10,000-sentence persistence regression proves zero full ReaderSnapshot construction during flush.
- The accepted A5 frame work remains: Arc-backed 104k catalog/document state, bounded pretty rendering with overscan, indexed highlight lookup, off-main TTS command submission, and optimized `profile.qa`.

Windows run `34280238462` tested implementation head `196dc387...` and passed native workspace check/build/test, QA preparation, goal-notification workflow validation, Windows TTS probe, and the hosted renderer probe.

### Remaining evidence

Automated A5/A5.1 correction is accepted. Gate 2.5 remains open only for the real-desktop scenario that exposed the defects:

1. pull accepted `main`;
2. run `.\qa.ps1` (now optimized by default);
3. open the same large Recent/Caliberate EPUB;
4. verify hover/button response is immediate enough for normal native use;
5. scroll the pretty view and verify it remains responsive;
6. press Windows TTS Play and confirm the process stays alive and speech begins;
7. verify next/previous/pause/resume/stop.

If the native reader remains multi-second-laggy, the pretty virtualization/projection is still insufficient. If Play crashes or stalls, A5.1 signoff fails. Do not advance to Gate 3 until this succeeds.

## Historical decision — attempt A4

**ACCEPTED AND INTEGRATED.**

Accepted worker terminal head:

`84a163b137c6209f80262a4992306b9c2c72c3f3`

Authoritative A4 implementation commit:

`5d9397ca3dbb0e172e04f68757e0d8b0fbb1c8e8`

Authoritative Windows CI:

`34260546736`

### Director findings

A4 directly repairs the real-desktop stall localized after A3:

- `load_session_for_source_with_cancel` no longer synchronously precomputes normalization for every page/sentence before returning the reader session;
- EPUB repagination and sentence-anchor setup remain in the bounded source-open path;
- idle `ReaderSnapshot` construction avoids creating a normalization/TTS plan when TTS is not active;
- first TTS activation lazily prepares a bounded 64-display-sentence plan window around the active sentence rather than the complete one-page EPUB;
- bounded plan windows preserve display/audio mapping and rebuild as the cursor crosses the active window;
- sentence-addressable normalization cache files are populated only for the bounded active window rather than thousands of sentences during open;
- `TextNormalizer` now owns a `CompiledNormalizer` built at config load/default construction, covering configured abbreviation regexes, case/nocase abbreviation tokens, brand/custom pronunciation patterns, acronym matchers, and year matching;
- invalid configured abbreviation regexes remain warned/skipped rather than crashing load.

The deterministic 3,500-sentence regression proves:

1. idle snapshot leaves `current_plan` absent;
2. idle open creates no normalization cache directory/files;
3. first Play creates at most one 64-sentence plan window plus its page-plan cache;
4. 100 next-sentence transitions cross plan-window boundaries and preserve the expected display cursor.

The branch also preserves A3 Caliberate EPUB materialization/native-ingestion/cache-recovery coverage and existing non-PDF/runtime suites.

Windows run `34260546736` tested implementation head `5d9397ca...` and passed both `native-workspace` and `hosted-renderer-probe`. The native job passed repo-native QA preparation, workspace check, workspace build, normal workspace test, and the Windows TTS probe.

A parallel local workspace run reproduced the repository's known shared-cache race in an unrelated recent-book test; the authoritative Windows job's normal workspace test passed, and no unrelated recent-cache code changed in A4.

### Remaining evidence

A4 implementation is accepted. Gate 2.5 still requires one real-desktop verification because the human machine exposed both prior failures that CI could not:

1. pull accepted `main`;
2. run `.\qa.ps1` with Caliberate already running;
3. open one real Caliberate EPUB and confirm the reader appears promptly;
4. immediately exercise Windows TTS Play, next, previous, pause/resume, and stop.

If reader startup still stalls, Goal 0008 reopens again. If the reader appears but TTS stalls or breaks at runtime, treat that as A4 signoff failure rather than advancing to Gate 3.

## Historical decision — attempt A2

**ACCEPTED AND INTEGRATED at the time, before real-desktop EPUB verification.**

Goal 0008 is complete after correction attempt A2.

Implementation branch:

`codex/0008-caliberate-service-integration`

Terminal head:

`d75aa8d0000c199eb23a5fefdba964b08ef27eb7`

Integrated main commit:

`61cf661d51d324ecf395e231a564d1a845fea522`

Authoritative Windows CI:

`34185172624`

## Accepted architecture

LanternLeaf now has one library browser with a provider boundary:

```text
library browser/UI
  -> provider
      -> Caliberate HTTP/JSON v1
      -> legacy Calibre content-server compatibility
  -> materialized local source
  -> existing LanternLeaf source/session/TTS pipeline
```

Caliberate is the preferred/default local provider at:

`http://127.0.0.1:8181`

Legacy Calibre remains available explicitly.

## Accepted Caliberate behavior

- paged `GET /api/v1/books`;
- bounded page size;
- deterministic title ordering;
- author/year/format/size mapping;
- deterministic allowed-format priority;
- `primary_format` fallback;
- unsupported-format filtering;
- optional `x-api-key` authentication;
- `GET /api/v1/books/{id}/content/{format}` materialization;
- temporary file + atomic rename;
- Caliberate-specific download namespace;
- no legacy cover endpoint probing;
- actionable service errors.

## Canonical reader handoff

The existing egui effect path remains authoritative:

`OpenCalibreBook -> materialize_book_path -> open_source_from_path`

Caliberate does not introduce a second reader/session/TTS state machine.

## Provider/cache isolation

Attempt A2 repaired the director-found stale-cache issue.

Catalog cache now:

- is scoped by provider and cache signature in its path;
- serializes provider identity;
- rejects cross-provider stale fallback;
- retains same-provider fallback behavior.

Deterministic regression:

`calibre::catalog::tests::stale_fallback_never_crosses_provider_boundary`

passed on the authoritative Windows run.

## Legacy Calibre compatibility

Attempt A2 added an end-to-end mock HTTP regression proving:

- `/interface-data/books-init?...library_id=...`;
- row mapping;
- `/get/EPUB/{id}/{library_id}`;
- downloaded bytes;
- Basic authentication.

Regression:

`calibre::catalog::tests::legacy_http_catalog_materialization_and_basic_auth_remain_functional`

passed on the authoritative Windows run.

## Validation

Authoritative Windows run `34185172624` passed:

- repo-native dependency/bootstrap path;
- Windows prerequisites;
- QA preparation;
- goal notification workflow tests;
- `cargo check --workspace`;
- `cargo build --workspace`;
- `cargo test --workspace`;
- Windows TTS probe;
- hosted renderer capability probe.

Core suite:

**180 passed / 0 failed**

Touched Rust files are rustfmt-clean and `git diff --check` passed. Repository-wide rustfmt still has known unrelated pre-existing drift and was not broadened into this goal.

## Human verification

No additional implementation goal should be queued before real-machine verification.

Next human run:

```powershell
git switch main
git pull --ff-only
.\qa.ps1
```

with Caliberate already running at `127.0.0.1:8181`.

Verify:

1. catalog populates from the real Caliberate library;
2. a supported book opens;
3. the book enters the normal reader;
4. Windows TTS speaks that Caliberate-sourced book;
5. any visible/interactive defects are reported back for director triage.

## Final status

**Goal 0008 COMPLETE. Gate 2.5 implementation complete; real-desktop service/reader/TTS signoff pending.**


## Post-acceptance real-desktop hotfix — automatic catalog load

The first real desktop run after Goal 0008 acceptance exposed a workflow defect not covered by the provider tests: the native starter shell rendered an empty library and did **not** issue `LoadCalibreBooks` until the human pressed Refresh manually.

The Caliberate service itself was already running; the app simply never requested the catalog during native startup.

Hotfix:

- native starter startup now dispatches:
  - `Bootstrap`;
  - `RefreshRecents { limit: None }`;
  - `LoadCalibreBooks { force_refresh: false }`;
- a regression test asserts the startup command set includes the automatic library load;
- `qa.ps1` now stages `conf/calibre.toml` into the isolated QA config and explicitly sets `CALIBRE_CONFIG_PATH`, removing reliance on current-working-directory config discovery.

Windows native CI for the hotfix passed dependency setup, QA preparation, workspace check/build/test, and Windows TTS.

Real-desktop Caliberate catalog population remains the next human verification.
