# Goal 0008 — A8.3 Final Director Acceptance

Status: **COMPLETE. Goal 0008 / Gate 2.5 is accepted on automated + real-desktop evidence.**

Accepted worker terminal head: `89952461f3d03177bb5c7460e9d1d4e9b17e6f6b`

Accepted implementation: `771c31ae90e0bf331195979fffebac8eb2f4ffd6`

Authoritative Windows CI: `34431615684`

Director integration/QA handoff: `306cd9e5d362b7649ec20a6cf41748ec4f57eb4b`

## Automated evidence

A8.3 closed the rendered-coordinate defects identified in A8.2 while preserving the accepted A5–A8.2 performance, canonical-session, Windows-TTS, structured-provenance, audio-boundary, and repaint work.

- Structured source extraction and native pretty adaptation share one canonical visible-text whitespace policy.
- Structured sentence ranges are applied after rendered spans enter the same coordinate stream; regressions cover multibyte text, `&nbsp;`, repeated whitespace, tabs/newlines, nested styles, and inline-image splitting.
- One canonical sentence may retain multiple rendered pretty segments with one deterministic primary follow target.
- Native pretty and text-only projection prefer high-frequency canonical playback identity over stale full-document page/local state.
- Remote persistence keeps page-local bookmark identity distinct from canonical/global playback identity.
- Structured EPUB/native HTML uses one explicit logical page for the current pretty-mode ownership model.
- First-sample `SentenceStarted` boundaries, active egui repaint scheduling, bounded pretty virtualization, one canonical shared `ReaderSession`, snapshot-free hot paths, and Windows TTS behavior remain intact.

Windows workflow `34431615684` passed both `native-workspace` and `hosted-renderer-probe`, including repo-native QA preparation, staged Windows synthesis/decode, workspace check/build/test, Windows TTS probe, and renderer probe.

## Final real-desktop evidence — 2026-09-10

The same large Caliberate/Recent EPUB that exposed the prior failures now passes the hard product path on the human Windows machine:

- source opens quickly and native UI interaction is snappy;
- Windows audio plays successfully;
- Play and Pause work;
- installed Windows voices can be changed successfully during the reader session;
- pretty-view highlight tracks the actually spoken sentence accurately;
- pretty-view highlight stays synchronized with TTS through sustained playback;
- automatic page/viewport scrolling follows playback correctly in both pretty and text-only views.

This is sufficient to close Goal 0008 because its central outcome is now demonstrated end-to-end:

`Caliberate -> materialized EPUB -> fast native reader -> Windows speech -> canonical sentence boundary -> accurate pretty highlight + synchronized follow`

## Known residuals transferred to Goal 0009

These are real defects, but they no longer invalidate the Caliberate integration / pretty synchronization outcome:

1. **Occasional unsolicited sentence replay.** Roughly once every several spoken lines, audio may repeat the sentence it just completed. The pretty highlight remains synchronized with the repeated audio, strongly localizing the remaining defect to audio preparation/queue/refill/control rather than canonical visual synchronization.
2. **Text-only visual highlight is missing/broken.** Text-only auto-scroll follows the correct spoken sentence, so canonical playback identity/follow is working there; the remaining defect is visual row highlighting.
3. **Piper live-switch recovery is not robust on Windows.** Selecting Piper without a usable local Piper setup can make TTS unusable for that reader session even though the app survives. Piper exists as a backend; full Piper model/voice provisioning is not part of Goal 0008.
4. **Voice configuration inheritance needs explicit layering.** New books should inherit an app-level Windows Zira preference; an explicit voice/backend selection for one book should persist as a book-level override without freezing unrelated global app settings.

Goal 0009 owns those residuals. Do not reopen Goal 0008 for them unless a regression breaks the now-accepted Caliberate/pretty synchronization path.