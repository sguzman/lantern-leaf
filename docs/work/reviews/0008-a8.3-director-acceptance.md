# Goal 0008 — A8.3 Director Acceptance

Status: **ACCEPTED FOR REAL-DESKTOP QA; Goal 0008 remains open pending human synchronization signoff.**

Accepted worker terminal head: `89952461f3d03177bb5c7460e9d1d4e9b17e6f6b`

Accepted implementation: `771c31ae90e0bf331195979fffebac8eb2f4ffd6`

Authoritative Windows CI: `34431615684`

## Director findings

A8.3 closes the rendered-coordinate defects identified in the A8.2 review while preserving the accepted A5–A8.2 performance, canonical-session, Windows-TTS, structured-provenance, audio-boundary, and repaint work.

- Structured source extraction and native pretty adaptation now share an explicit canonical visible-text whitespace policy.
- Structured sentence ranges are applied only after rendered spans are canonicalized into that same coordinate stream; tests assert UTF-8-safe boundaries and exact reconstructed highlighted text across multibyte text, `&nbsp;`, repeated whitespace, tabs/newlines, nested inline styles, and inline-image splitting.
- One canonical sentence may retain multiple rendered pretty segments, with a deterministic primary segment for follow/scroll.
- Native pretty and text-only projection prefer the high-frequency canonical playback identity over stale full-document page/local state.
- Remote persistence no longer relabels a page-local bookmark sentence as a canonical/global sentence ID.
- Structured EPUB/native HTML remains one explicit logical page for current pretty-mode ownership; the prior word-budget repartition inference is removed.
- Existing source-block lineage, first-sample `SentenceStarted` boundary identity, active egui repaint scheduling, bounded pretty virtualization, canonical shared ReaderSession, snapshot-free hot paths, and Windows TTS behavior remain intact.

Windows workflow `34431615684` passed both `native-workspace` and `hosted-renderer-probe`, including QA preparation, staged Windows synthesis/decode, workspace check/build/test, Windows TTS probe, and renderer probe.

## Remaining human evidence

Run the normal repo-native Windows QA path and use the same large Caliberate/Recent EPUB that previously exposed the synchronization failure. Verify sustained playback without touching the mouse/keyboard: text-only and pretty highlighting should follow the actually audible sentence; pretty viewport should follow without random jumps; Pause should freeze the sentence; Resume should wait for the next actual audio boundary; Next/Previous/Repeat should preserve visible/audible sentence identity; switching text-only/pretty during playback should preserve the same canonical sentence; Jump to highlight should return to the active sentence after manual scrolling.

If this passes, Goal 0008 / Gate 2.5 can close. If it fails, capture the normal QA log and report the observed failure; do not start another Codex correction until director diagnosis.